BUGS

- Minspeed is not honored.
- Memory leak in cbot-info-shell using readline library.

Warning: some part of code are experimentals or not fully integrated.

NEW FEATURES

- Memory management while downloading (buffers during download)
- Accept IP ranges for a country.
- IPv6 protocol *.
- HTTP/3 or at least HTTP/2 protocol.
- HTTP pipelining.
- Linear Probe Deleting *.
- Assignment of a higher score to sites with the https protocol.
- Assignment of a higher scores to pages that can be reached through other web pages dealing with the same thematics.
- Using the anchor text of hypertext links as additional text to pages accessed through them.

* It was a work in progress.
